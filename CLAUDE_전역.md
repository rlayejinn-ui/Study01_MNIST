# 전역 CLAUDE.md

모든 프로젝트에서 Claude Code가 공통으로 따르는 개인 설정입니다.
프로젝트 폴더의 CLAUDE.md가 이 파일과 다르게 정하면 프로젝트 쪽을 따릅니다.

## 언어

- 설명과 답변은 **한국어**로 합니다.
- 코드를 새로 작성할 때는 코드·주석·변수명·함수명·화면 문구를 한글로 씁니다.
  외부 라이브러리·API 이름과, 문법상 영문만 허용되는 곳은 예외입니다.
- 커밋 메시지는 한글로 씁니다.

## 작업 방식

- 작업이 끝나면 실제로 실행·검사해서 동작을 확인한 뒤 결과를 알려 줍니다.
  확인하지 못한 부분이 있으면 그렇다고 분명히 말합니다.
- 공개 저장소에 푸시, GitHub 설정 변경, 배포처럼 밖으로 나가는 작업은
  요청받은 범위만 하고, 범위가 애매하면 먼저 물어봅니다.
- 이미 있는 파일이나 저장소를 덮어써야 할 때는 내용을 먼저 확인하고 물어봅니다.

## 환경 (Windows 11)

- Python은 `%LOCALAPPDATA%\Programs\Python\Python312\python.exe` (py 런처 있음).
  `python` 명령은 Microsoft Store 바로가기라 쓰지 않습니다. 프로젝트마다 `.venv`를 만들어 씁니다.
- 한글 출력이 깨지면 `PYTHONIOENCODING=utf-8`을 지정합니다.
- GitHub CLI는 `C:\Program Files\GitHub CLI\gh.exe`. 새로 연 터미널이 아니면 PATH에 없을 수 있습니다.
- `gh auth login`처럼 입력을 기다리는 명령은 앱의 터미널 패널에서 끊기기 쉬우므로,
  백그라운드로 실행해 기기 코드를 알려 주는 방식으로 진행합니다.
